{
  "name": "nukit-open-air-purifier-builder",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "typecheck": "tsc --noEmit",
    "build": "bun run typecheck && vite build",
    "test": "bun test",
    "port:audit": "bun scripts/boxes-port/boxes-port-audit.ts",
    "oracle:airpurifier": "bun scripts/boxes-port/airpurifier-oracle.ts",
    "preview": "vite preview --host 127.0.0.1"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "@types/three": "^0.184.0",
    "bun-types": "^1.3.13",
    "typescript": "^5.9.3",
    "vite": "^7.3.0"
  },
  "dependencies": {
    "three": "^0.184.0"
  }
}
